---
'use-everywhere-codemod': minor
---

First release. `npx use-everywhere-codemod rename-1.0 src/` rewrites a `0.x` codebase to the 1.0 names, including the `StoreHooks.get()` and `ChannelHooks.useMessage` member renames. `--dry-run` lists what would change without writing anything.
