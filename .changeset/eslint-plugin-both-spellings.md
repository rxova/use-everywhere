---
'eslint-plugin-use-everywhere': minor
---

`define-at-module-scope` and `no-dynamic-name` now match `createStoreHooks` as well as `defineStore`, so a codebase part-way through the 1.0 renames is checked either way. Rule ids and the recommended config are unchanged.
