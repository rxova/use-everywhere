---
'eslint-plugin-use-everywhere': major
---

1.0. `define-at-module-scope` and `no-dynamic-name` match `createStoreHooks` instead of `defineStore`. Rule ids and the recommended config are unchanged.
