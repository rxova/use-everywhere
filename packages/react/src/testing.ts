/**
 * Test seams for multi-tab tests, re-exported so React apps still need only
 * one dependency. Same contents as '@use-everywhere/core/testing'.
 *
 * ```ts
 * import { MemoryHub } from 'use-everywhere/testing';
 * ```
 */
export { MemoryHub, MemoryTransport } from '@use-everywhere/core/testing';
