---
'eslint-plugin-use-everywhere': major
---

1.0, alongside the libraries. `define-at-module-scope` and `no-dynamic-name` now match `createStoreHooks`, the 1.0 name of `defineStore`, and no longer match the removed name. Rule ids, the recommended config and the other two rules are unchanged.
